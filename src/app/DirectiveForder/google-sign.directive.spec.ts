import { GoogleSignDirective } from './google-sign.directive';

describe('GoogleSignDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleSignDirective();
    expect(directive).toBeTruthy();
  });
});
