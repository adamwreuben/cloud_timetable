import { YahooDirective } from './yahoo.directive';

describe('YahooDirective', () => {
  it('should create an instance', () => {
    const directive = new YahooDirective();
    expect(directive).toBeTruthy();
  });
});
